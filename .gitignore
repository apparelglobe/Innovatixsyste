node_modules/
.next/
dist/
.turbo/
out/
*.log

# env
.env
.env.local
.env.*.local

# local dev database cluster (brew Postgres, no Docker)
.pgdata/
.devdata/

# database dumps produced by scripts/backup-db.sh (never commit backups)
backups/

# generated Prisma client
**/generated/prisma/

# local portal file storage (dev)
.filestore/

# ── OS / editor / build caches / backups (Priority Zero hygiene) ──
.DS_Store
*.tsbuildinfo
next-env.d.ts
*.bak
# local PM2 runtime state (the ecosystem.config.js source IS committed)
.pm2/
